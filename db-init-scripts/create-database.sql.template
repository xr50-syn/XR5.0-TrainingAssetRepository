-- Create the first database and user
CREATE DATABASE IF NOT EXISTS `{{XR50_REPO_DB_NAME}}`;
CREATE USER IF NOT EXISTS '{{XR50_REPO_DB_USER}}'@'%' IDENTIFIED BY '{{XR50_REPO_DB_PASSWORD}}';
CREATE USER IF NOT EXISTS '{{XR50_REPO_DB_USER}}'@'localhost' IDENTIFIED BY '{{XR50_REPO_DB_PASSWORD}}';
GRANT ALL PRIVILEGES ON *.* TO '{{XR50_REPO_DB_USER}}'@'%' WITH GRANT OPTION;
GRANT ALL PRIVILEGES ON *.* TO '{{XR50_REPO_DB_USER}}'@'localhost' WITH GRANT OPTION;

-- Apply changes
FLUSH PRIVILEGES;
