# XR5.0 Training Asset Repository Environment Configuration

# Application Environment
ASPNETCORE_ENVIRONMENT=Development

# Database Configuration (Main Application Database)
XR50_REPO_DB_NAME=xr50_repository
XR50_REPO_DB_USER=xr50admin
XR50_REPO_DB_PASSWORD=hub_XR50

# Storage Configuration - Set the storage backend to use
# Options: "S3", "OwnCloud", "MinIO"
STORAGE_TYPE=S3

# === S3 Configuration (used when STORAGE_TYPE=S3) ===
# AWS S3 Settings
AWS_HOST=http://localstack:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=eu-west-1

# === MinIO Configuration (used when STORAGE_TYPE=MinIO) ===
# MinIO Settings (for local S3-compatible testing)
MINIO_ENDPOINT=http://minio:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# === OwnCloud Configuration ====
# OwnCloud Database Settings
OWNCLOUD_DB_USER=emmie
OWNCLOUD_DB_PASSWORD=!@m!nL0v3W!th@my

# OwnCloud Admin Settings
OWNCLOUD_ADMIN_USER=emmie
OWNCLOUD_ADMIN_PASSWORD=!@m!nL0v3W!th@my

# OwnCloud Network Settings
OWNCLOUD_TRUSTED_DOMAINS=localhost,owncloud,amethyst,amy.library.lab.synelixis.com,192.168.190.33
# OwnCloud API Endpoints
OWNCLOUD_BASE_API=http://192.168.190.33:8080
OWNCLOUD_BASE_WEBDAV=http://192.168.190.33:8080/remote.php/webdav
OWNCLOUD_GROUPS_PATH=/ocs/v1.php/cloud/groups
OWNCLOUD_USERS_PATH=/ocs/v1.php/cloud/users

# === Development/Testing Configuration ===
# Uncomment the storage type you want to test with:

# For OwnCloud testing (default):
#STORAGE_TYPE=OwnCloud

# For MinIO testing:
# STORAGE_TYPE=MinIO

# For real S3 testing (requires valid AWS credentials):
# STORAGE_TYPE=S3
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
